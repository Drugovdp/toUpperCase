String.prototype.toJadenCase = function () {
    return str.split(' ').map(e => e.split('').map((k, j) => j == 0 ? k.toUpperCase() : k).join('')).join(' ')
};

let str = "How can mirrors be real if our eyes aren't real";
str.toJadenCase();
